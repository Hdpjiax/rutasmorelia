/**
 * This URL must point to the versioned style generated from the same OSM PBF
 * used by Valhalla. CARTO remains an explicit fallback while that style is not
 * deployed; route geometry never depends on this URL.
 */
export const MAP_STYLE_URL =
  'https://basemaps.cartocdn.com/gl/positron-gl-style/style.json';

