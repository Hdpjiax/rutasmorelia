{
  "name": "rutas-morelia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "npm --prefix apps/api run start:dev",
    "dev:mobile": "npm --prefix apps/mobile run start",
    "build:web": "npm --prefix apps/web run build",
    "build:api": "npm --prefix apps/api run build",
    "check": "npm --prefix apps/web run lint && npm --prefix apps/web run build && npm --prefix apps/api run lint && npm --prefix apps/api test -- --runInBand && npm --prefix apps/api run build && npm --prefix apps/mobile run typecheck"
  }
}
